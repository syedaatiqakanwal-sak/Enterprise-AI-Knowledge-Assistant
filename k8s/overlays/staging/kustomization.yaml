apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
patches:
  - target:
      kind: ConfigMap
      name: eai-config
    patch: |-
      - op: replace
        path: /data/ENVIRONMENT
        value: staging
namePrefix: staging-

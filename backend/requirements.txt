# =============================================================================
# Module 2A — Backend Core Infrastructure
# Python 3.12 | FastAPI | SQLAlchemy 2.0 Async | Alembic | Motor | Redis
# =============================================================================

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
httpx==0.27.0

# Settings & validation
pydantic[email]==2.7.4
pydantic-settings==2.3.3
python-dotenv==1.0.1
email-validator==2.2.0

# PostgreSQL (async)
sqlalchemy[asyncio]==2.0.31
asyncpg==0.29.0
greenlet==3.0.3
alembic==1.13.1

# MongoDB (async)
motor==3.5.0
pymongo==4.8.0

# Redis (async)
redis==5.0.7

# Security foundations (auth endpoints land in a later module)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3

# Logging
colorlog==6.8.2

# Observability (Module 12)
prometheus-client==0.20.0
psutil==6.0.0

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0

# Module 6 — RAG (lightweight; heavy ML models in requirements-ai.txt)
qdrant-client==1.9.1
pypdf==4.2.0
python-docx==1.1.2
python-pptx==0.6.23
openpyxl==3.1.5

# Module 7 — OCR / Vision (lightweight core; Paddle/YOLO in requirements-ai.txt)
numpy==1.26.4
Pillow==10.4.0
